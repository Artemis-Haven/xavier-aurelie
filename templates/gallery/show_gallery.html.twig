{% extends 'base.html.twig' %}

{% block body %}
    <h1>Galerie de photos</h1>
	
	{% for photo in gallery.photos %}
		<a href="{{ path('homepage') }}">{{ photo.id }}</a>
	{% endfor %}
{% endblock %}

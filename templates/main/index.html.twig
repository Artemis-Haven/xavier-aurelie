{% extends 'base.html.twig' %}

{% block body %}

    {% if title|trim is not empty %}
		<h1>{{ title|nl2br }}</h1>
    {% endif %}
    {% if legend|trim is not empty %}
		<h4>{{ legend|nl2br }}</h4>
    {% endif %}

	<div class="row">
		<div class="col-md-6">
			{{ textarea|nl2br }}
			<br><br>
			<img src="{{ asset('images/index.png') }}" alt="Mariage" class="img-responsive img-thumbnail">
		</div>
		<div class="col-md-6">
			<h3>Dernières nouvelles</h3>
			{% include 'blog/_article_preview.html.twig' with {'article': lastBlogArticle} only %}
		</div>
	</div>
{% endblock %}
